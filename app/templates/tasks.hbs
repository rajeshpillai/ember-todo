{{outlet}}

<h3>Tasks</h3>
{{#each model as |task|}}
  <div class="well">
    <h4>{{#link-to 'tasks.edit' task.id}} {{task.id}} {{/link-to}}</h4>
    Title: {{task.title}}
    <small>Due: {{format-date task.date}} </small>
    <p>{{task.description}}</p>

    <button {{action 'deleteTask' task.id}}class="btn btn-danger">delete</button>

  </div>
{{/each}}
